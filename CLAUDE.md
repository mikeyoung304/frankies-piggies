# Piggy World

@AGENTS.md

(AGENTS.md is the source of truth for this repo — it applies to every agent
harness, so nothing Claude-specific lives here. The `@` line above imports it by
mechanism so Claude Code always loads it, not just when it chooses to.)
